import { EmployeefiterPipe } from './employeefiter.pipe';

describe('EmployeefiterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeefiterPipe();
    expect(pipe).toBeTruthy();
  });
});
